{
  "name": "6ad",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "clean": "rm -rf apps/*/.next apps/*/.turbo .turbo",
    "dev": "turbo run dev",
    "dev:fresh": "npm run clean && turbo run dev",
    "build": "npm run db:generate && turbo run build",
    "start": "node apps/api/dist/index.js",
    "lint": "turbo run lint",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:admin": "npm run dev --workspace=apps/admin",
    "dev:agency": "npm run dev --workspace=apps/agency",
    "dev:user": "npm run dev --workspace=apps/user",
    "db:generate": "cd packages/database && npx prisma generate",
    "db:push": "cd packages/database && npx prisma db push",
    "db:studio": "cd packages/database && npx prisma studio",
    "db:seed": "dotenv -e .env.test -- npx tsx packages/database/prisma/seed.ts",
    "dev:test": "dotenv -e .env.test -- turbo run dev",
    "postinstall": "npm run db:generate"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "turbo": "^2.0.0"
  },
  "dependencies": {
    "bs58": "^6.0.0"
  }
}
